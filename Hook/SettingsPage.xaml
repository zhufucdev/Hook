<Page
    x:Class="Hook.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Hook"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Frame Padding="12">
        <Pivot x:Uid="SettingsPivot">
            <PivotItem x:Uid="GeneralPivot" Header="General">

            </PivotItem>
            <PivotItem x:Uid="ConverterPivot" Header="Converter">
                <ListView x:Name="ConverterList" SelectionMode="Single" IsItemClickEnabled="False">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:DocumentConvert">
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
                                <TextBlock Text="{Binding Name}" Style="{ThemeResource BaseTextBlockStyle}"/>
                                <TextBlock Text="{Binding Path}" Style="{ThemeResource BodyTextBlockStyle}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>                
            </PivotItem>
        </Pivot>
    </Frame>
</Page>
